(ns genetic.core)

(def target-string
  "west london hack")




(def letters
  (conj (map (comp char
                   (partial + 97))
             (range 26))
        \space))

(let [c (count target-string)]
  )

(shuffle) letters

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
